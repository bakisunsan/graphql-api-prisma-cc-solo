type Query {
  posts: [Post!]!
  post(id: String!): Post
  description: String!
}

type Mutation {
  createDraft(title: String!, content: String): Post
  deletePost(id: ID!): Post
  publish(id: ID!): Post
}

type Post {
  id: String!
  title: String!
  content: String!
  published: Boolean!
}
